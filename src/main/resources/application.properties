spring.application.name=nz-traffic-analysis-backend

# datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/user_dev
spring.datasource.username=pansai
spring.datasource.password=pansai

# jps
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# log
logging.level.root=INFO

# Spring Web - do not print the DTO
logging.level.org.springframework.web=INFO
logging.level.org.springframework.http=INFO
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestResponseBodyMethodProcessor=INFO

# Hibernate - only sql, no params
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=OFF

# self-written code - allow debug level output
logging.level.io.github.pansai.traffic=DEBUG

# output traceId
logging.pattern.console=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level %pid --- [%t] %logger{36} [traceId=%X{traceId}] : %msg%n


# email
app.mail.activation-base-url=http://localhost:5173
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pansai0128@gmail.com
spring.mail.password=hieb vqcw bvdy exzz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# jwt notice: app.jwt.type must be a space at the end
app.jwt.secret=${JWT_SECRET}
app.jwt.expire_minutes=120
app.jwt.type=Bearer 

